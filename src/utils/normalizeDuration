// utils/normalizeDuration.js
function normalizeDuration(value) {
  const d = Number(value);
  if (![1, 3, 6].includes(d)) {
    throw new Error('Invalid duration');
  }
  return d;
}

module.exports = normalizeDuration;
